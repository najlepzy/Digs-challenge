import React from "react";
import {
  LoaderContainer,
  LargeActivityIndicator,
} from "@/src/constants/Styled";

const Loader = () => (
  <LoaderContainer>
    <LargeActivityIndicator />
  </LoaderContainer>
);

export default Loader;
